import Foundation

// Placeholder bridge for future Kotlin Multiplatform framework import.
// Example after KMP integration:
// import shared
// let xp = SharedBalancing().requiredXp(level: 10)
enum KmpBridge {
    static func isSharedReady() -> Bool {
        return false
    }
}
